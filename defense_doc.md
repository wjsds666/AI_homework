# 简历智能匹配系统 · 答辩说明书

**T2-2 · Tier 2 综合层 · 第 5 组**

> 面向全体组员 · 零基础也能看懂 · 答辩不尬

---

## 这份文档怎么用

老师答辩时会问两类问题:① 模型原理(20 分);② 技术细节与设计决策(20 分)。**不会让你逐行念代码。** 所以这份说明书按「原理 70% + 架构 30%」来写——原理全用大白话和比喻讲清,每个点都配一句「**如果老师问\_\_,你就答\_\_**」,照着说就行。术语保留英文(老师认这个),但都会用人话解释一遍。

**建议:** 每个组员至少把「一、核心原理」和「五、预判问答」读熟,这两部分直接决定答辩分。

---

# 一、核心原理速通(最重要,占大头)

这一部分是答辩的命根子。把下面 4 个原理用自己的话讲出来,「解释模型原理 20 分」基本到手。

## 1. Sentence-BERT 是什么?为什么用它而不是普通 BERT?

**一句话:Sentence-BERT 是一个能把「一句话」变成「一串数字(向量)」的 AI 模型,意思相近的句子,变出来的数字也相近。**

打比方:想象给每句话在一张巨大的「语义地图」上标一个坐标点。「我精通 Python 后端开发」和「擅长用 Python 写服务端程序」这两句意思几乎一样,它们在地图上的点就挨得很近;而「我喜欢弹钢琴」就被甩到地图很远的角落。这个「把句子变成坐标点」的工具,就是 Sentence-BERT。

**为什么不用普通 BERT?**

- 普通 BERT 擅长「理解单个句子内部」,但你要它直接比较两句话像不像,得把两句拼在一起算一次——10 份简历配 1 个 JD 就要算很多次,慢得离谱。
- Sentence-BERT 是 BERT 的改良版(用了「孪生网络」结构,见第二部分 siamese),它能给每句话**单独**算好一个固定向量存起来,比较时只量两个向量的距离,极快。简历匹配天然是「多对多比较」,所以必须用它。

> 🎤 **如果老师问:** Sentence-BERT 和普通 BERT 有什么区别?
> 👉 **你就答:** 普通 BERT 比较两句话要把它们拼起来算一次,做 N 份简历就算 N 次,很慢;Sentence-BERT 能给每句话单独算一个向量存下来,比较时只量向量距离,又快又适合我们这种「一份 JD 对多份简历」的场景。它本质是用孪生网络结构微调过的 BERT。

## 2. 余弦相似度是什么?为什么能衡量「像不像」?

**一句话:余弦相似度就是看两个向量「方向」是不是一致——方向越接近,两句话越像。**

打比方:把每句话的向量想成一支从原点射出去的箭头。两支箭头指向几乎同一方向 → 夹角很小 → 意思很接近;两支箭头方向垂直甚至相反 → 夹角大 → 意思不相关。余弦相似度量的就是这个夹角:夹角 0° 得分 1(完全一致),90° 得分 0(毫不相关)。

公式(可放 PPT,但记住下面那句大白话就够答辩):

```
cos(A, B) = (A · B) / (|A| × |B|)    取值 -1~1,文本里通常 0~1
```

大白话:分子是两个向量的「点积」,分母把长度归一化掉,这样只剩「方向是否一致」这一个信息——不管箭头多长,只看它们指向多接近。我们代码里向量已经做了 L2 归一化,所以直接点乘就是余弦值。

> 🎤 **如果老师问:** 为什么用余弦相似度而不是直接算距离?
> 👉 **你就答:** 因为我们只关心两句话「意思方向」像不像,不关心向量长度。余弦只看方向夹角,天然忽略长度差异,对文本语义比较最合适。夹角越小、余弦越接近 1,就越相似。

<!-- PART1B -->
