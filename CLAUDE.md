# CLAUDE.md — 简历智能匹配系统(T2-2)

> 这是项目的**总控文档 + 进度追踪板**。每次新开 Claude 对话,先让我读这个文件,我就能立刻接上进度。
> 每完成一个里程碑,更新本文件的「当前进度」和「开发日志」两节。

> 📄 **要查评分细则/报告模板/时间表,读 [`评分指南-文本版.md`](评分指南-文本版.md),不要去解析那份 PDF**(PDF 用 CID 编码中文字体,标准工具读不了,需手写 CMap 解码脚本,每次都很费事)。原始要求见 [`作业要求.md`](作业要求.md)。

---

## 角色分工(重要,AI 读者先看这里)

> **给任何读到本文件的 AI:** 这份 CLAUDE.md 是**项目的共享说明**,会被本目录下所有对话自动加载。它**不是**某一个对话的私有指令。
> **你的职责由「用户这次让你做什么」决定,不由本文件决定。** 用户让你写代码,就写;让你做计划/追踪/写文字,就做。
> 下面的分工表是协作约定,帮你理解项目里有哪些角色——**不要据此拒绝用户的明确请求**(例如别因为看到「规划角色不写代码」就拒绝用户在本目录下让你写代码)。

本项目由三类「工作场」协作,各管各的,别混:

| 工作场 | 负责什么 | 典型触发指令 |
|---|---|---|
| **规划/追踪场** | 计划、进度追踪、评分对齐、写报告文字、**产出喂给 PPT 工具的内容大纲/讲稿(Markdown,非 .pptx)** | 「读CLAUDE.md做计划/更新进度/写报告/出PPT大纲」 |
| **代码场** | 实现 src/ 代码、跑实验、Streamlit Demo | 「读CLAUDE.md做阶段B/写parser.py/跑实验」 |
| **ppt-master**(`~/develope/开源项目/ppt-master`,独立 skill,有自己的 CLAUDE.md) | 把内容大纲/PDF → 生成**原生可编辑 .pptx** | 在 ppt-master 那边对话,不读本文件 |

**判断你自己是哪个角色:看用户这次的指令属于哪一列**,而不是看你在哪个对话。同一个项目目录里,规划和代码可以是不同对话,也可以是同一对话的不同阶段。

**PPT 流程**:规划/追踪场先产出「PPT 内容大纲(每页标题+要点+讲稿)」→ 用户把大纲丢给 ppt-master → ppt-master 生成 .pptx → 用户在 PowerPoint 里微调。
所以涉及 PPT 时,本文件这边交付的**永远是 Markdown 内容大纲**,不是 .pptx。

> ppt-master 形态备注:它是「harness,不是完整 agent」,在 AI IDE(Claude Code/Cursor 等)里用,推荐配 Claude 大窗口 + AI 生图。数据本地处理,输出真 DrawingML 可编辑 PPTX。用法见其仓库 `docs/zh/getting-started.md`。

---

## 0. 一句话项目定义

上传**简历 PDF/TXT** 和**岗位 JD**,系统输出**匹配分数(0-100)** + **「你缺少哪些技能」清单** + **已匹配技能清单**,并提供热力图等可视化分析。

- **课题编号**:T2-2 简历智能匹配系统
- **难度层级**:**Tier 2(综合层)** — 难度系数 ×1.15,得分上限 **100 分**
- **小组**:第 5 组(N=组内人数,贡献度表见报告附录 A)
- **目标定位**:冲高分(基础必做全做 + 多个加分项 + 至少 2 组对比实验)

---

## 1. 关键日期(今天 2026-06-03,务必倒排)

| 步骤 | 事项 | 日期 | 第5组安排 | 交付物 |
|---|---|---|---|---|
| 步骤2 | **开题汇报** | **6月4日(明天!)** | 课上 5 分钟 | 讲清「做什么/怎么做/预期效果」,开题PPT |
| 步骤3 | **技术进展汇报** | **6月9日** | 第1-10组当天 | 可运行核心代码 + **≥1组对比实验** + 讲核心原理,10分钟 |
| 步骤4 | **Demo 演示与总结** | **6月16日** | 第1-10组当天 | 现场运行 Demo(无明显Bug),10分钟 |
| 步骤5 | **最终材料提交** | **6月26日前** | — | 课题报告(PDF+Word) + 三次PPT + 完整源码.zip |

> 重要提示(来自评分指南):
> - 开题后原则上**不许换题**。
> - Demo 要求**现场运行**;若网络无法调 API,需准备**离线版本**(本项目用本地模型,天然离线)。
> - 技术进展汇报必须展示**可运行代码或初步实验结果**。

---

## 2. 评分体系(30-30-40,决定我们做什么)

最终成绩 = 基础原始分(满分100) × Tier系数(1.15)，封顶100。

| 阶段 | 权重 | 对应步骤 | 考核重点 |
|---|---|---|---|
| 过程追踪 | 30% | 步骤2、3 | 规划合理性、技术路线清晰、里程碑达成 |
| 现场答辩 | 30% | 步骤4 | Demo功能完整、模型原理与算法逻辑掌握 |
| 期末存档材料 | 40% | 步骤5 | 文档规范、**实验对比分析深度**、功能实现程度、问题记录与个人总结 |

### 拿分关键点(逐条对应,写代码/文档时回头核对)

**过程追踪(30%)**
- 开题(50%):目标明确且关联课程知识点(20)/ 技术路线可行+时间规划合理(15)/ 预期效果可量化可验证(15)
- 技术进展(50%):核心代码已实现并运行(20)/ **≥1组对比实验**(15)/ 能解释关键技术原理(15)

**现场答辩(30%)**
- Demo完整运行无明显Bug(25)/ 清晰解释模型原理(20)/ 回答技术细节提问(20)/ 实验对比有数据支撑(20)/ 时间控制+表达(15)

**期末存档(40%)**
- 文档规范(20):结构完整(引言/方法/实验/结论/参考文献) + README有运行说明 + 架构图/数据流图齐全
- **实验对比分析深度(30)**:Tier2 须 **≥2组对比实验**,结果用表格图表呈现,有明确结论解释差异原因 ← **拿分大头**
- 功能实现(25):基础必做全实现 + 加分项酌情 + 可复现稳定
- 问题解决与总结(25):真实记录 **≥3个Bug** 及解决路径 + 个人总结 + 改进方向

### Tier2 图表硬指标
- 须有 **1张模型架构图** + **≥2张实验结果图**(Loss曲线/对比柱状图等)
- 在 Tier1 基础上增加**可视化分析图**(特征图、热力图、混淆矩阵等)
- 「图表比文字重要,分析比描述重要,原创比引用重要」

---

## 3. 功能清单(基础必做 + 加分项)

### 基础必做(及格线,全做)
1. 解析简历和 JD 文本(支持 **PDF 和 TXT**)
2. 用 **Sentence-BERT**(如 all-MiniLM-L6-v2)提取句向量
3. 计算整体**语义相似度(余弦相似度)**
4. **关键词匹配**:提取 JD 技能词,检查是否在简历中出现
5. **综合打分**:语义相似度 60% + 关键词匹配 40%
6. 输出:**匹配分数 + 缺失技能清单 + 已匹配技能清单**

### 加分项(冲高分,按性价比排序)
- [P0] **匹配热力图**:简历每句 × JD 每句 相似度矩阵,matplotlib 热力图 ← 同时满足Tier2可视化硬指标
- [P0] **可视化雷达图**:技能/经验/学历/项目 各维度匹配度
- [P1] **批量简历排序**:上传多份简历,按匹配度排名(满足「批量」演示效果)
- [P1] **行业分类**:先判断简历属于 IT/金融/教育等,再匹配对应 JD
- [P2] **手写 Siamese Network 训练**:最难加分项,需理解孪生网络,用 Colab/Kaggle GPU 训练 ← 答辩「能解释原理」的大杀器

> 对比实验设计(满足 ≥2组,直接喂给报告实验章节):
> 1. **不同嵌入模型对比**:all-MiniLM-L6-v2 vs all-mpnet-base-v2 vs paraphrase-multilingual(中文)— 比匹配准确度/速度
> 2. **打分权重对比**:纯语义 vs 纯关键词 vs 60/40 融合 — 看哪种排序更合理
> 3.(可选)**自训练 Siamese vs 预训练**:证明微调收益

---

## 4. 技术栈与环境

- **语言**:Python 3.13(本机已装)
- **核心库**:`sentence-transformers`、`scikit-learn`(余弦相似度)、`pdfplumber`(PDF解析)、`keybert`(关键词提取)、`streamlit`(界面)、`matplotlib`/`plotly`(可视化)
- **界面框架**:**Streamlit**(最快搭出可上传PDF+显示分数+热力图的Demo,适合现场答辩)
- **嵌入模型**:默认 `all-MiniLM-L6-v2`(轻量,CPU可推理,离线)
- **算力**:本地 CPU 跑推理;**Siamese 训练用 Colab(免费T4,每天约12h)或 Kaggle(每周30h)**
- **硬件结论(已确认)**:整个主系统是**纯 CPU 推理,不需要 GPU**。`all-MiniLM-L6-v2` 仅约80MB/22M参数,CPU 编码一份简历一两秒即可。**唯一需要 GPU 的是可选加分项 Siamese 训练**——在 Colab/Kaggle 上训练,产出权重(几十MB)拉回本地,推理仍在 CPU。训练与部署完全分离,服务器永不碰训练。
- **部署目标机:4G 内存轻量云服务器(无 GPU),够用**。全栈加载约 1.5-2GB,4G 有余量。省内存两点(写代码时注意):①KeyBERT 复用已加载的 all-MiniLM-L6-v2,别另拉模型;②`torch.set_num_threads(2)`(轻量云通常2核)。
- **HuggingFace 国内镜像**(若下载模型超时):
  ```python
  import os; os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'
  ```
  备选:ModelScope(魔搭社区)下载同款模型。

> ⚠️ 当前 pip 在沙箱里走代理被 403 拦截,联网装包可能失败。装依赖时若失败,在普通终端(非沙箱)里 `pip install` 或用国内镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple`。

### 部署决策(Docker)
- **目标环境**:国内服务器(阿里/腾讯/华为云)→ 镜像内 pip 用国内源、模型走 hf-mirror。
- **基础镜像**:`python:3.11-slim`(不用本机3.13,torch/sentence-transformers 在3.11生态最稳)。
- **模型策略**:**构建时把 all-MiniLM-L6-v2 烘进镜像**(`docker build` 阶段预下载到 `/models`),容器启动即用、完全离线,Demo 答辩最稳。
- **暴露方式**:裸端口 **8501**(Streamlit 默认),无鉴权、无反向代理(作业Demo/内网场景)。
- **编排**:`docker compose up` 一键起。
- **不提交大文件**:`.dockerignore` 排除 `data/`(真实数据)、`.git`、模型缓存等;模型在构建时下载,不入 git。

---

## 5. 目标目录结构(实现时按此建)

```
人工智能导论/
├── CLAUDE.md                      # 本文件:总控 + 进度追踪
├── 作业要求.md                     # 原始课题要求(T2-2)
├── 评分指南-文本版.md
├── 人工智能导论期末项目执行与评分指南.pdf
├── requirements.txt               # 依赖锁定(torch 2.12.0 等,含 pytest)
├── README.md                      # 运行说明 + Docker部署(评分项!)✅
├── Dockerfile                     # ✅ python:3.11-slim,构建时烘模型到 /models,离线运行
├── docker-compose.yml             # ✅ 一键起,8501:8501,platform amd64,mem_limit 2g
├── .dockerignore                  # ✅ 排除 .git/data/venv/缓存/results/docs
├── app.py                         # ✅ Streamlit 主界面入口(端口8501,AI报告式深色UI)
├── .streamlit/config.toml         # ✅ 锁深色主题(投屏视觉一致)
├── src/
│   ├── __init__.py                # ✅
│   ├── config.py                  # ✅ feature flags 开关(默认全关+环境变量覆盖)
│   ├── parser.py                  # ✅ 简历/JD 解析(PDF+TXT)
│   ├── embedder.py                # ✅ Sentence-BERT 句向量(本地目录优先)
│   ├── matcher.py                 # ✅ 余弦相似度 + 综合打分(+句×句矩阵+句向量)
│   ├── skills.py                  # ✅ 技能词典提取 + 匹配/缺失(173技能)
│   ├── visualize.py               # ✅[加分] 热力图(HEATMAP)/ 雷达图(RADAR)
│   ├── classifier.py              # ✅[加分] 行业分类(INDUSTRY_CLASSIFY)
│   └── siamese/                   # ✅[加分] 孪生网络空骨架(不训练,留Colab)
│       ├── __init__.py
│       ├── model.py
│       ├── train.py               # 在 Colab/Kaggle 跑
│       └── dataset.py
├── experiments/
│   ├── __init__.py                # ✅
│   ├── exp1_model_compare.py      # ✅ 对比实验1:嵌入模型(已出表+图)
│   ├── exp2_weight_compare.py     # ✅ 对比实验2:打分权重(csv+3图+结论)
│   └── results/                   # ✅ 图表输出(exp1+exp2 共7个产物)
├── data/
│   ├── resumes/                   # ✅ 3份脱敏假简历(后端/数据/前端)
│   └── jds/                       # ✅ 2份假JD(后端/数据)
├── docs/                          # [待建] report.md / 架构图.png / ppt/
├── tests/
│   ├── __init__.py                # ✅
│   └── test_matcher.py            # ✅ 打分逻辑单测(10 passed)
└── venv/                          # 本地虚拟环境(.dockerignore 已排除,勿入 git)
```


---

## 6. 当前进度(★ 每次完成里程碑后更新这里)

**最后更新**:2026-06-06

### 阶段总览
- [x] **M0 需求理解 + 立项文档**(本 CLAUDE.md)— ✅ 2026-06-03
- [x] **M1 开题材料**(6月4日前)— ✅ 开题PPT大纲完成,已汇报
- [x] **M2 核心MVP**(6月9日前)— ✅ 2026-06-06 基础必做6项跑通 + exp1对比实验 + Streamlit上传出分 + pytest 7 passed
- [x] **M3 加分项 + 实验**(6月16日前)— ✅ 2026-06-06 四加分项(热力图/雷达图/批量/行业分类,feature flag 默认关)+ exp2 权重对比 + Siamese空骨架;pytest 9 passed
- [ ] **M4 Siamese训练**(可选,6月16日前)— Colab训练孪生网络,对比预训练
- [ ] **M5 报告与存档**(6月26日前)— 报告(7章)+ README + 三次PPT + 源码zip + 贡献声明表
- [ ] **M6 云端部署**(正式目标,上云求稳)— Docker 已就绪,剩断网离线验证 + 推服务器实跑 + 防火墙/鉴权

### 详细 checklist(基础必做)
- [x] parser.py:PDF 解析(pdfplumber)— 代码完成,语法校验通过
- [x] parser.py:TXT 解析(UTF-8/GBK 兜底)
- [x] embedder.py:加载 all-MiniLM-L6-v2 + 句向量(本地目录优先 + hf-mirror 兜底)
- [x] matcher.py:整体余弦相似度(+ 句×句矩阵备热力图)
- [x] skills.py:KeyBERT 提取 JD 技能词 — 改为技能词典扫描为主(见 Bug #9),KeyBERT 降级为可选演示
- [x] skills.py:简历命中检测 → 已匹配/缺失清单(提取与匹配共用别名规则)
- [x] matcher.py:综合打分(语义60% + 关键词40%)
- [x] app.py:Streamlit 上传 + 展示分数/清单(端口 8501)
- [x] ✅ **运行时验证**:venv 装包 + 下模型 + `streamlit run app.py` 实测;pytest 7 passed

### 详细 checklist(加分项)
- [x] config.py:feature flag 开关机制(默认全关 + 环境变量覆盖 + sidebar 勾选)— ✅ 2026-06-06
- [x] visualize.py:句×句相似度热力图(HEATMAP flag)— ✅ 复用 sim_matrix,matplotlib
- [x] visualize.py:四维度雷达图(RADAR flag)— ✅ 技能/经验/学历/项目,plotly 懒加载
- [x] app.py:批量简历上传 + 排序(BATCH_RANK flag)— ✅ 逐份处理省内存
- [x] classifier.py:行业分类 IT/金融/教育等(INDUSTRY_CLASSIFY flag)— ✅ 关键词词典法
- [x] siamese/:孪生网络**空骨架**(model/dataset/train)— ✅ 仅结构+TODO,训练留 Colab(M4)

### 详细 checklist(交付/实验)
- [x] experiments/exp1:嵌入模型对比 — ✅ 已跑通,生成 csv + 2张png 到 results/
- [x] experiments/exp2:打分权重对比 — ✅ 2026-06-06 纯语义/纯关键词/60-40融合,出 csv+3图+结论(区分度:纯语义6.9/纯关键词86.6/融合38.8)
- [ ] docs/架构图.png
- [x] README.md(运行说明 + Docker部署节)— ✅ 2026-06-06
- [ ] docs/report.md(7章:引言/方法/实验/问题记录/总结/参考文献/附录)
- [x] Bug记录 ≥3条(开发中实时记到第8节)— ✅ 已记 14 条(超额)
- [ ] 成员贡献声明表(附录A,全员签字)

### 详细 checklist(Docker 部署 — 第二步)
- [x] Dockerfile:python:3.11-slim + 清华pip源/apt源 + 构建时烘 all-MiniLM-L6-v2 到 /models + EXPOSE 8501 + HEALTHCHECK
- [x] docker-compose.yml:8501:8501 + restart + 挂载./data + mem_limit 2g(`docker compose config` 已校验语法)
- [x] .dockerignore:排除 .git/data/venv/缓存/results/docs
- [x] README Docker部署节 + 公网安全警告
- [x] ⏳ **构建验证**(普通终端):`docker compose -p resume-matcher up -d --build` 成功,容器 Up(healthy),health 返回 ok ✅
- [ ] ⏳ **离线交互验证**(需普通终端重建镜像):加了 config/classifier/visualize 后须重新 `--build`;断网后浏览器上传简历+JD 出分。代码层已确认:`COPY src/` 覆盖全部新文件、plotly 已入 requirements、flag 默认全关(Demo 镜像干净)

---

## 7. 分阶段路线图(每阶段对应一次或多次 Claude 对话)

> 每个阶段是一个**自包含的对话目标**。新对话直接说「做阶段X」,我读完本文件就知道上下文。

### 阶段 A — 开题材料(对话1,今天就做,赶6月4日)
- 产出开题汇报**内容大纲(Markdown:每页标题+要点+5分钟讲稿)**,讲清做什么/怎么做/预期效果。
- 这份大纲交给 **ppt-master** 生成 .pptx(本对话不出 PPT 文件)。
- 不需要写完整代码,但要把技术路线图、模块划分、预期可量化指标(如「Top-1排序准确率」)讲清楚。

### 阶段 B — 核心 MVP(对话2-3,赶6月9日技术进展)
- 搭项目骨架(目录、requirements.txt)。
- 实现基础必做 1-6 全部 + Streamlit 界面能上传PDF出分。
- 跑通**第1组对比实验**(嵌入模型对比),出初步图表。
- 验证:本地 `streamlit run app.py` 能上传样例简历+JD,显示分数和缺失技能。

### 阶段 C — 加分项 + 第2组实验(对话4-5,赶6月16日Demo)
- 热力图、雷达图、批量排序、行业分类。
- 第2组对比实验(打分权重),凑齐 Tier2 的 ≥2组实验。
- Demo 打磨:准备 2-3 份样例数据,排练现场流程,确保无明显Bug。

### 阶段 D — Siamese 训练(对话6,可选,GPU)
- 写孪生网络 model/train/dataset,在 Colab 跑。
- 拿训练好的权重回填,做「自训练 vs 预训练」对比(第3组实验)。

### 阶段 E — 报告与存档(对话7-8,赶6月26日)
- 按 7 章模板写报告(引言/技术方案/实验与结果/问题解决/总结/参考文献/附录)。
- 整理架构图、所有实验图表。
- 写 README、打包源码 zip、填贡献声明表。
- 报告 Markdown → Word + PDF。
- 三次汇报的 PPT:本对话产出**内容大纲**,交 ppt-master 生成 .pptx。

### 阶段 F — 云端部署(正式目标:上云求稳,非作业硬性要求但已决定做)
- **目标**:把系统稳定挂在 4G 内存轻量云服务器(无 GPU)上长期运行。
- Docker 化已基本完成(见第6节 Docker checklist),剩:① 手工断网离线交互验证;② 推到服务器实跑。
- 服务器部署步骤:装 Docker/compose → 传代码 → `docker compose -p resume-matcher up -d --build`(首次构建会下模型,较慢)→ 访问 `http://服务器IP:8501`。
- **上线前务必**:配防火墙/安全组只放行 8501 给可信来源,或加反向代理+鉴权(裸端口无保护,别公网全开)。
- 长期稳定项:`restart: unless-stopped` 已配;`mem_limit 2g` 已配;关注服务器内存占用,必要时加 swap。

---

## 8. 开发日志 / Bug 记录(★ 实时记,报告第4章直接用)

> 评分硬性要求:真实记录 **≥3个** 开发中的 Bug/技术难题,每条含:**现象 → 原因 → 解决方案 → 经验教训**。
> 开发时一旦踩坑,立刻记在这里,别等到最后补。

### 决策记录
- 2026-06-03:确定 Tier2、第5组、Streamlit界面、冲高分目标、本地CPU+Colab GPU。
- 2026-06-06:**确定上云,目标稳定运行**。Docker 不是作业要求(课堂Demo本机streamlit即可),但用户要把系统挂到 4G 内存轻量云服务器长期自用,故 Docker 化升格为正式阶段 F。硬件已确认:主系统纯 CPU 推理,4G 无 GPU 够跑;GPU 仅 Siamese 训练用(在 Colab,不在服务器)。
- 2026-06-06:**加分项采用 feature flag 策略**。四个纯推理加分项(热力图/雷达图/批量排序/行业分类)全部实现并入库(源码有=报告可写=拿"加分项酌情给分"),但 **UI 默认关闭**,Demo 只跑稳定核心链路(保 Demo 无 Bug 的 25 分),需要时用开关临时展示。开关机制:`src/config.py` 集中管理 feature flags(默认全 False),可被环境变量覆盖;另在 Streamlit sidebar 放「高级功能」折叠区可临时勾选开启。Siamese 训练(M4)需 Colab GPU,今天不做训练,最多搭骨架。
- 2026-06-06:**语义分重标定 + 热力图换条形图**(基于实测反馈)。① 语义余弦对「都是职场文本」普遍偏高(同属IT的错配也能到 0.85+),直接当百分制使错配分虚高;改为减基线 0.65 再拉伸到 0~100(`rescale_semantic`),错配明显掉下来、对口仍高,exp2 区分度结论更强(纯语义 6.9→19.7)。② 句×句热力图信息过载且长标签溢出,换成「JD 每条要求→简历最佳匹配句相似度」横向条形图(`coverage_bar_figure`),更聚焦更易读。③ Streamlit 用 session_state 管理结果,上传/改 JD 后旧结果失效并提示重算。
- 2026-06-06:**UI 重构为「AI 分析报告」式深色界面**(纯展示层,后端零改动)。新增 `.streamlit/config.toml` 锁深色主题(投屏一致);app.py 用统一设计 token(CSS 变量:主色/成功/警示/卡片/圆角/阴影)。信息层级:品牌条 → STEP1 输入卡 → STEP2 分析报告(结果摘要 Hero=当前输入+一句话结论+综合分大环 / 三指标卡 / 自然语言总结 / 技能卡硬技能优先)→ STEP3 深度分析(行业/雷达/JD满足度,二级模块不抢焦点)。新增纯展示 helper:`verdict_text`(分数→结论)、`summary_text`(自然语言总结)、`sort_skills`(硬技能优先)。侧栏缩窄、高级功能默认折叠、文案统一专业中文。
- 2026-06-06:本机仅有 Python 3.13(无 pyenv/conda/3.11),Docker 目标是 3.11。
  requirements.txt 选用同时支持 3.11/3.13 的 CPU 版本组合,torch 最终固定 2.12.0
  (2.5.x 在 3.13 无 wheel,见 Bug #3),一份锁定文件同时服务本地验证与 Docker 镜像。

### Bug / 难题记录
- **Bug #1:Homebrew Python 拒绝 pip 直装(PEP 668 externally-managed)**
  - 现象:`pip3 install -r requirements.txt` 报 `error: externally-managed-environment`。
  - 原因:Homebrew 的 Python 按 PEP 668 标记为「外部管理」,禁止往全局环境直接装包,防止破坏 Homebrew。
  - 解决方案:用虚拟环境隔离 —— `python3 -m venv venv && source venv/bin/activate` 后再装包。
  - 经验教训:macOS + Homebrew 下务必先建 venv,别用 `--break-system-packages` 硬来。

- **Bug #2:沙箱/代理环境下 pip 走清华源被 403 拦截**
  - 现象:`pip install ... -i https://pypi.tuna.tsinghua.edu.cn/simple` 反复
    `ProxyError: Tunnel connection failed: 403 Forbidden`,5 次重试全失败。
  - 原因:受限网络的出站代理只放行白名单主机,清华 PyPI 镜像与 hf-mirror 均不在白名单内。
  - 解决方案:在**普通终端(非受限网络)**里装包;若公司/学校代理仍拦,临时 `unset http_proxy https_proxy`
    或换可访问镜像。模型下载同理,设 `HF_ENDPOINT=https://hf-mirror.com`,必要时改走 ModelScope。
  - 经验教训:CI/沙箱里的网络限制要早暴露;真正的「离线 Demo」靠第二步把模型烘进 Docker 镜像,从根上规避现场网络问题。

- **Bug #3:torch==2.5.1 在 Python 3.13 上无可用 wheel**
  - 现象:`pip install` 报 `Could not find a version that satisfies the requirement torch==2.5.1`,
    PyPI 对 3.13 可见的 torch 从 `2.6.0` 起步。
  - 原因:torch 2.5.x 发布时尚未提供 cp313 轮子;Python 3.13 需 torch ≥ 2.6.0。
  - 解决方案:requirements.txt 把 torch 固定到 `2.12.0`(3.13 实测装通)。⚠️ 3.11 上该版本待第二步 docker build 验证。
  - 经验教训:锁版本前先确认目标解释器有对应 wheel;跨 3.11/3.13 双环境时,torch 这类 C 扩展包最易踩版本/ABI 坑。

- **Bug #4:pip 对 PyPI/清华源 SSL 证书校验失败**
  - 现象:装包时 pip 对清华源与官方 PyPI 均报 SSL 证书校验错误,无法建立连接。
  - 原因:本机/网络环境的 CA 证书链不完整或被中间代理替换,导致 TLS 校验不通过。
  - 解决方案:临时加 `--trusted-host pypi.tuna.tsinghua.edu.cn --trusted-host pypi.org --trusted-host files.pythonhosted.org` 绕过校验完成安装。
  - 经验教训:`--trusted-host` 是应急手段(等于跳过 TLS 校验,有中间人风险),根治应修复系统 CA 证书(如 macOS 上跑 `Install Certificates.command`)。

- **Bug #5:hf-mirror 对当前 huggingface_hub 组合未返回模型元数据**
  - 现象:跑 exp1 时设了 `HF_ENDPOINT=https://hf-mirror.com`,但该镜像对当前 huggingface_hub 版本没成功返回模型元数据,模型拉取失败。
  - 原因:hf-mirror 与某些 huggingface_hub 版本的 API/元数据接口存在兼容性间隙(镜像未完全跟上官方接口)。
  - 解决方案:改用官方 Hugging Face 预下载完整模型权重后,实验顺利跑通。
  - 经验教训:hf-mirror 是兜底而非银弹;最稳的离线方案仍是第二步把模型预下载烘进 Docker 镜像(`RESUME_MODEL_DIR` 指向本地目录),运行时彻底不依赖任何线上元数据接口。

- **Bug #6:docker-compose 把 platform 写在 build 下导致校验失败**
  - 现象:`docker compose config` 报 `services.app.build additional properties 'platform' not allowed`。
  - 原因:Compose 规范里跨架构构建/运行的 `platform` 是 **service 顶层**字段,不是 `build` 子字段(易与 `docker buildx --platform` 混淆)。
  - 解决方案:把 `platform: linux/amd64` 从 `build:` 下移到 `app:` service 顶层,`docker compose config` 通过。
  - 经验教训:写完 compose 先 `docker compose config` 静态校验语法(不连 daemon 也能跑),能在真正 build 前抓出字段位置错误。

- **Bug #7:构建期清华 apt 镜像源返回 403**
  - 现象:Dockerfile 里把 Debian apt 源换成清华镜像后,`apt-get update` 在构建期返回 403,装 curl 失败,构建中断。
  - 原因:部分网络/时段下清华 Debian 镜像对该构建环境拒绝访问(与第一步 pip 403 同源问题:第三方镜像不稳定)。
  - 解决方案:Dockerfile 改回 **Debian 官方源**直接 `apt-get install curl`,构建通过。
  - 经验教训:镜像源是「提速兜底」,不保证比官方稳;构建脚本应让源可切换,遇 403 能快速回退官方源。

- **Bug #8:docker compose 缺省项目名报 project name must not be empty**
  - 现象:`docker compose up -d --build` 报 `project name must not be empty`。
  - 原因:某些环境下 compose 无法从当前目录名推断项目名(目录含中文「人工智能导论」可能触发)。
  - 解决方案:显式指定项目名 `docker compose -p resume-matcher up -d --build`(后续 ps/logs 也带 `-p resume-matcher`)。
  - 经验教训:目录名含非 ASCII 时,compose 命令统一显式带 `-p <项目名>`,避免推断失败。

- **Bug #9:中文 JD 下「已匹配技能」恒为 0(核心业务逻辑 bug)**
  - 现象:Demo 上传简历+JD,无论多对口,「已匹配技能」永远是 0,命中率 0%。
  - 原因(两个叠加):
    1. 旧 skills.py 用 KeyBERT + `keyphrase_ngram_range=(1,2)` 从**中文 JD** 提技能。中文无空格分词,
       KeyBERT 把「中文动词+英文技能」当成一个 2-gram,提出 `'熟练掌握 java'`、`'熟悉 docker'`
       这类被中文污染的长短语,而非干净技能名。
    2. 匹配逻辑对含空格的短语走「整串子串匹配」,简历里写的是「精通 Python 与 Java」,
       不可能包含「熟练掌握 java」这一整串 → 全部 miss → 恒为 0。
  - 解决方案:改「技能词典扫描」为主(`SKILL_DB`:规范名→别名),提取与匹配**共用同一套别名规则**;
    纯 ASCII 词用词边界匹配(java 不误命中 javascript),含 `+#./` 或中文的别名用子串匹配。
    KeyBERT 降级为可选的 `extract_keyphrases`(仅报告演示,不进主链路)。
  - 验证:后端→后端、数据→数据 命中率 100%;前端→后端仅命中 Git(7%),区分度正确;新增 2 条回归单测,9 passed。
  - 经验教训:简历匹配里「技能」是有限可枚举的专有名词,词典扫描比通用关键词抽取更准更可控;
    中文 NLP 场景慎用依赖空格分词的英文向 NLP 工具。**提取与匹配必须共用同一套规则**,否则「提得出却匹配不上」。

- **Bug #10:词典法的覆盖面 tradeoff —— 非 IT 岗位提取技能过少**
  - 现象:上传真实「高级产品经理(SaaS)」JD,只提取到 SQL + 数据分析 2 个技能,已匹配/缺失都极少。
  - 原因:Bug #9 改用的 `SKILL_DB` 初版只覆盖 IT/开发/数据方向(Python、Docker、MySQL…),
    产品 JD 的核心技能(需求分析、原型设计、Axure/Figma、竞品分析、用户画像、留存率、NPS…)词典里没有,
    只碰巧扫到跨领域重合的 SQL、数据分析。这是「词典法」的固有取舍:准,但覆盖不到的领域提不出来。
  - 解决方案:扩充 SKILL_DB 至 **173 个技能**,覆盖:IT开发/AI大数据/测试运维云/前端移动端,
    及产品、项目管理、运营市场、设计、财务金融、人力行政、法律、医疗、教育、销售客服、通用软技能等多行业。
    产品 JD 重测:提取技能 2 → 26 个;别名无冲突,回归单测 9 passed。
  - 经验教训:词典法的上限取决于词典广度,需按业务覆盖的岗位类型持续维护;
    长期可考虑「词典 + KeyBERT 兜底未登录词」的混合策略,兼顾准确率与覆盖率。
  - 补充(多行业实测 + 误报修复):用产品/财务/HR/AI/后端 5 个跨行业(简历×JD)实例验证,
    对口岗综合分 83~90、关键词命中 100%,不对口(后端→财务)仅 17.9 分、关键词 0,区分度正确。
    实测中发现「招聘」别名过宽:JD 标题「招聘XX经理」里的"招聘"被所有岗位误当成技能。
    已收紧为需 HR 语境(人才招聘/校招/社招/招聘渠道/负责招聘等),裸"招聘"不再误报。

- **Bug #11:exp2 用 Top-1 准确率衡量打分策略,小数据上失效**
  - 现象:exp2 对比纯语义/纯关键词/60-40融合,三者 Top-1 排序准确率都是 100%,看不出谁好谁坏,结论空洞。
  - 原因:数据集只有 3 简历×2 JD,对口简历本就最突出,任何策略都能排第一 → Top-1 这个指标分辨率不够。
  - 解决方案:补「区分度」指标 = 对口简历均分 − 非对口均分。结果一目了然:
    纯语义 6.9(几乎分不开)、纯关键词 86.6(最大但脆弱)、60-40融合 38.8(平衡)。结论由此有数据支撑。
  - 经验教训:小样本评估别只看准确率(易饱和到 100%),要选**有分辨率的连续指标**(分差/AUC/边际)才能体现差异。

- **Bug #12:可视化加分项首版观感差(热力图含噪声、雷达图空洞/掉0)**
  - 现象:Demo 实测发现 ① 热力图坐标轴塞了「姓名/电话/邮箱/岗位职责:/任职要求:」等个人信息与章节标题,既无意义又挤压内容;② 雷达图只有一条光秃轮廓无参照无读数;③ 雷达「项目」维常为 0。
  - 原因:① 热力图直接拿全部句子,没过滤简历个人信息行与 JD 章节标题行;② 雷达维度分用「简历句必须含特定锚词才算分」,锚词太窄,匹配不到就 0(如「主导SaaS产品从0到1」未命中项目锚词)。
  - 解决方案:① 热力图加噪声过滤(联系方式无条件滤、短标题句滤)+ 按相关度排序把强匹配集中左上 + 每格标数值 + 红绿配色;② 雷达改为「维度描述句 × 简历句」语义相似度取 top-k 均值,任何维度都有合理分值,并叠加满分基准环 + 数值标注 + 平均分标题。
  - 经验教训:可视化是 Demo 门面,数据要先洗(去噪声)再展示;评分逻辑用「软语义相似度」比「硬关键词命中」更鲁棒,不会因措辞差异掉 0。复用已加载 embedder 编码维度描述,不另拉模型(守内存铁律)。

- **Bug #13:Streamlit 换文件后旧结果不失效 + 错配语义分虚高(实测反馈)**
  - 现象:① 出分后换上传另一份简历,页面仍显示旧分数,不点按钮不更新,易误导;② 错配(产品简历×后端JD)综合 54.5、语义 86.5,看着「还不错」,可信度存疑;③ 按钮无明确加载/完成反馈,结果区会整块消失。
  - 原因:① Streamlit 每次交互全脚本重跑,结果只在点按钮那一帧算,没用 session_state 持久化也没做输入变更检测;② Sentence-BERT 文档级平均池化对「都是职场文本」普遍给高余弦,直接当百分制 → 错配虚高(平均池化放大泛化词)。
  - 解决方案:① 用 `st.session_state` 存结果+输入指纹(文件名/大小/JD文本),换文件或改 JD 即判定过期、显示「输入已变更,请重新匹配」,加分项均基于已存结果重算;② 语义分减基线 0.65 重标定(`rescale_semantic`);③ 改用 `st.status` 显示「解析→编码→计算→完成」并保留结果。
  - 验证:对口 88~93,错配前端/产品×后端 20~47,明显拉开;单测 10 passed;exp2 区分度结论仍成立。
  - 经验教训:Streamlit 这类「全脚本重跑」框架,凡是「上次算的结果」都要进 session_state 并配输入指纹失效,否则状态会骗用户。语义相似度作分数前要按领域基线重标定,余弦绝对值不能直接当百分制。

- **Bug #14:「客户成功」「销售」等职责段描述被误当技能(实测反馈)**
  - 现象:产品 JD 匹配后,缺失技能里出现「客户成功」。该词来自 JD 职责段「配合销售团队完成定价策略、竞品分析及**客户成功**案例沉淀」——这里「客户成功」是「成功案例」的一部分,不是 Customer Success 岗位技能;「销售」同理是部门名不是技能要求。
  - 原因:`extract_skills` 扫**整篇 JD**,把「岗位职责」段的描述性文字也当技能来源。职责段描述岗位干什么(噪声多),真正对候选人的技能要求在「任职要求」段。
  - 解决方案:加 `_requirement_section`,技能提取**只从「任职要求/岗位要求/qualifications」等小标题之后的段落**扫描;无明确分段时回退全文(不漏)。
  - 验证:同份产品 JD,「客户成功/销售/商业化/定价策略」等职责段噪声不再入选,任职要求段真技能(SQL/需求分析/Axure/Figma…)全保留;新增回归单测,11 passed。
  - 经验教训:简历匹配里「JD 要候选人具备什么」≠「这个岗位干什么」,技能提取要锚定**任职要求段**而非整篇,从源头滤掉职责描述噪声,比逐个给歧义词打别名补丁更治本。

> **第二步构建验证结论(2026-06-06,普通终端实跑)**:
> `docker compose -p resume-matcher up -d --build` 成功,容器 `Up (healthy)`,
> `curl http://localhost:8501/_stcore/health` 返回 `ok`。
> 镜像内容大小约 **750MB**,Docker Desktop 磁盘占用约 **3.16GB**(含层/缓存)。
> Server 实为 linux/arm64(本机 M1);推腾讯云 x86 时须按 README 用 `--platform linux/amd64` 重建。
> 模型烘焙最终走官方 `HF_ENDPOINT=https://huggingface.co`(hf-mirror 仍受 Bug #5 影响)。
> 「断网手工上传」交互验证尚未做(需切断本机网络在浏览器实操),但模型已在构建期烘入、运行时健康检查通过,离线链路设计成立。

---

## 9. 如何用 Claude 多对话、多阶段完美完成本项目

> 这是给你(用户)的协作说明书。

### 9.1 核心机制:CLAUDE.md = 跨对话记忆
- Claude 每开一个**新对话会失忆**,但本项目所有上下文都沉淀在这份 CLAUDE.md 里。
- **每次新对话第一句话**:`读 CLAUDE.md,我们做阶段X`(或直接说要做的事)。我会先读文件接上进度,不用你重新解释项目。
- **每个阶段结束**:让我「更新 CLAUDE.md 的进度和开发日志」。文档始终是最新的事实来源。

### 9.2 推荐的对话节奏(一个阶段 = 一个对话)
1. **对话1(今天)**:`读 CLAUDE.md,帮我准备明天的开题汇报` → 产出**开题PPT内容大纲(交ppt-master生成)**。
2. **对话2-3**:`读 CLAUDE.md,做阶段B,搭骨架并实现基础必做`(代码对话) → MVP 跑通。
3. **对话4-5**:`读 CLAUDE.md,做阶段C的加分项和第2组实验`(代码对话)。
4. **对话6**:`读 CLAUDE.md,做阶段D的Siamese训练`(给Colab notebook)。
5. **对话7-8**:`读 CLAUDE.md,做阶段E,写报告`;PPT大纲交ppt-master。

> 为什么分对话?单个对话太长会被压缩、容易跑偏;一个对话聚焦一个阶段,质量最高,且每阶段都有可验证的产出。
> 分工按**指令类型**走(见顶部「角色分工」):规划/追踪/写文字 是一类;写代码/跑实验 是另一类;**.pptx** 交给 ppt-master。同一目录下这几类可以是不同对话,也可以同一对话切换——以用户当次指令为准。

### 9.3 让我表现最好的几个习惯
- **每阶段先对齐再写**:复杂阶段我会先给计划,你确认后我再动手,避免白做。
- **要求我验证**:写完代码我会跑 `streamlit run` / 单测确认能用,而不是只说「写好了」。
- **及时记 Bug**:踩坑当场让我记进第8节,报告第4章就不用临时编。
- **小步提交**:每完成一块让我更新进度 checklist,你随时能看到完成度。
- **真实数据脱敏**:测试用造的简历数据,别放真实姓名/电话/身份证(PII)。

### 9.4 常用指令模板
```
读 CLAUDE.md,我们继续做阶段B。先给我计划再动手。
读 CLAUDE.md,实现 src/parser.py 的 PDF 解析,跑通后更新进度。
读 CLAUDE.md,跑对比实验1,把图表存到 experiments/results/。
读 CLAUDE.md,根据现在的代码和实验,写报告的实验章节。
更新 CLAUDE.md:勾掉已完成项,补今天的 Bug 记录。
```

---

## 10. 风险与注意事项
- **6月4日开题在即**:优先级最高是阶段A,先把汇报材料搞定,代码可以之后补。
- **联网装包/下模型可能被墙**:用 hf-mirror / ModelScope / 清华pip镜像。模型首次下载较慢,提前下好缓存,Demo现场用本地缓存(离线)。
- **Tier2 拿分大头是「实验对比分析深度(30分)」**:别只堆功能,务必做满 ≥2组对比实验且有图表+结论。
- **答辩占30%**:要能讲清 Sentence-BERT 原理、余弦相似度、(若做)孪生网络原理。代码不是写完就行,要理解。
- **贡献声明表**:报告附录A必填,全员签字,贡献度总和精确=100%。
